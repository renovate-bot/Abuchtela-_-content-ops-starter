{
  "tasks": {
    "test": "echo \"No test scripts defined in package.json\"",
    "build": "nvm install && nvm use && npm install && npm run build",
    "launch": "nvm install && nvm use && npm install && npm run dev"
  }
}